---
interface Props { title: string; eyebrow?: string; description?: string }
const { title, eyebrow, description } = Astro.props;
---
<header class="page-header">{eyebrow && <div class="eyebrow">{eyebrow}</div>}<h1>{title}</h1>{description && <p>{description}</p>}</header>
